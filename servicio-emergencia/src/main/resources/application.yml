server:
  port: 8082 # Puerto para este microservicio
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ # URL del Servidor de Descubrimiento

spring:
  cloud:

    compatibility-verifier:
      enabled: false # Deshabilita la verificación que causa el fallo de arranque.
    # ---------------------------------------------------